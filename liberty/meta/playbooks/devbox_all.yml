---

- name: Install & build all development dependencies
  hosts: localhost
  roles:
    - compilation_tooling
    - folly
    - nvml
    - wangle # depends on folly being first
    - libbloom
    - re2
    - hyperloglog
    - librdkafka
    - cityhash
    - cassandra_client
